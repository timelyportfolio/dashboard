/** @jsx React.DOM */

var React = require('react');

var Dashboard = module.exports = React.createClass({
  render: function() {
    return (
      <div>
        <h2>Dashboard</h2>
      </div>
    );
  }
});

